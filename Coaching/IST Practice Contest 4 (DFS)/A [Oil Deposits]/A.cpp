#include <bits/stdc++.h>
#In input
#Inf -1

using namespace std;

int __row, __col, __grid[101][101];

int main(){
	
	for(In >> __row >> __col; __row != 0 && __col != 0; In >> __row >> __col){

		for(i = 1; i <= __row; i++) for(j = 1; j <= __col; j++) __grid[i][j] == Inf;

	}
	
	return 0;
}
